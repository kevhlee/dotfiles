#!/usr/bin/env python3

import random
import sys

if __name__ == '__main__':
    args = sys.argv[1:]
    if len(args) == 0:
        print("Bro, there's nothing to choose from.")
        exit(1)
    print(random.choice(sys.argv[1:]))
